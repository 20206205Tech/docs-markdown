Viewed money.spec.ts:44-44

Đây là một yêu cầu rất thiết thực. Việc gặp lỗi lặp đi lặp lại thường do thiếu context về cấu trúc thư mục, quy ước đặt tên, hoặc các dependencies đặc thù của dự án.

Để giúp tôi (hoặc bất kỳ AI nào khác) làm việc hiệu quả hơn, bạn nên cung cấp một **"Developer Guideline Prompt"** ngay từ đầu phiên làm việc. Dưới đây là gợi ý Prompt tối ưu cho dự án này:

---

### 🚀 Optimized AI Prompt for Payment Service

**Hành động: Hãy dán đoạn này vào tin nhắn đầu tiên khi bắt đầu một phiên làm việc mới.**

> **Context Dự án:** Đây là dự án Payment Service chạy trên NestJS, kiến trúc DDD (Domain-Driven Design).
> **Quy tắc thực hiện lệnh (Command Guidelines):**
> 1. **Môi trường:** Luôn dùng lệnh `doppler run -- [lệnh]` để đảm bảo có đầy đủ biến môi trường. Ví dụ: `doppler run -- npm run test`.
> 2. **Chạy Test:** Khi sửa một file, hãy chạy test cụ thể của file đó trước để tiết kiệm thời gian: `doppler run -- npx jest path/to/file.spec.ts`.
> 3. **Kiểm tra File:** Trước khi sửa (modify), LUÔN LUÔN dùng `view_file` hoặc `list_dir` để xác nhận đường dẫn và nội dung hiện tại. Không được đoán mò đường dẫn (ví dụ: `@20206205tech/nestjs-common` hay `common` module).
> 4. **Kiểm tra Exception:** Các custom exception nằm trong `src/modules/payment/domain/exceptions`. Base exception là `DomainException` từ `@20206205tech/nestjs-common`.
>
> **Quy tắc Code (Coding Standards):**
> - **Transactional Outbox:** Tuyệt đối không lưu Outbox thủ công trong Event Handler. Logic này phải nằm trong Repository (sử dụng uncommitted events).
> - **Idempotency:** Mọi Domain Service (như `PaymentDomainService`) phải có check trạng thái trước khi thực hiện logic (ví dụ: `if (txn.isPending())`).
> - **Test Quality:** Không được tạo test rỗng. Phải mock đầy đủ các Ports/Adapters (Kafka, Supabase, Brevo).
>
> **Ví dụ lệnh ưu tiên:**
> - Kiểm tra lỗi nhanh: `doppler run -- npm run lint`
> - Chạy test module cụ thể: `doppler run -- npx jest src/modules/payment/domain`
> - Chạy app dev: `doppler setup --project 20206205tech --config dev && doppler run -- npm run start:dev`

---

### Tại sao Prompt này giúp tôi chạy nhanh hơn?

1.  **Tránh "Blind Edits":** Quy tắc số 3 buộc tôi phải nhìn trước khi nhảy, tránh việc sửa sai đường dẫn hoặc sai property name (như lỗi `.value` vs `.amount` lúc nãy).
2.  **Atomicity trong lệnh:** Việc nhắc nhở về `doppler` giúp tránh các lỗi "Missing Environment Variable" khiến lệnh sụp đổ ngay lập tức.
3.  **Localize Testing:** Thay vì chạy `npm run test` (mất 15-30s cho 90 suites), AI sẽ chỉ chạy test cho phần nó vừa sửa (mất 2-3s), giúp vòng lặp phản hồi nhanh hơn nhiều.
4.  **Kiến trúc đồng nhất:** Việc nhắc nhở về Transactional Outbox giúp AI không đi chệch khỏi thiết kế ban đầu của bạn.

Bạn có thể lưu đoạn Prompt trên vào một file `AI_INSTRUCTIONS.md` trong root dự án để mỗi khi cần, chỉ cần yêu cầu AI: *"Hãy đọc file AI_INSTRUCTIONS.md và tuân thủ các quy tắc trong đó"* nhé!
